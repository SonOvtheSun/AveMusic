<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="
             http://maven.apache.org/POM/4.0.0
             https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.avemonica.avemusic</groupId>
    <artifactId>avemusic-cloud</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>AveMusic Cloud</name>
    <description>
        基于自研 MiniRPC 的分布式音乐播放平台
    </description>

    <properties>
        <java.version>17</java.version>

        <mini-rpc.version>1.2.1-SNAPSHOT</mini-rpc.version>

        <spring-boot.version>4.1.0</spring-boot.version>
        <mybatis-spring-boot.version>4.1.0</mybatis-spring-boot.version>

        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <maven.compiler.parameters>true</maven.compiler.parameters>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <mybatis-plus.version>3.5.17</mybatis-plus.version>
    </properties>

    <modules>
        <module>avemusic-common</module>
        <module>avemusic-music-api</module>
        <module>avemusic-music-provider</module>
        <module>avemusic-gateway</module>
        <module>avemusic-user-api</module>
        <module>avemusic-user-provider</module>
        <module>avemusic-file</module>
    </modules>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <dependencyManagement>
        <dependencies>

            <!--
                MiniRPC BOM 内部已经导入
                OpenTelemetry Instrumentation BOM。
            -->
            <dependency>
                <groupId>com.avemonica.minirpc</groupId>
                <artifactId>mini-rpc-bom</artifactId>
                <version>1.2.1-SNAPSHOT</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!--
                只有在 AveMusic 自己显式导入
                Spring Boot BOM 时才需要这一段。
                MiniRPC BOM 必须放在它前面。
            -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>4.1.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <release>${java.version}</release>
                        <encoding>UTF-8</encoding>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>